#include <stdio.h>
#include <stdlib.h>

// Definizione del nodo della lista concatenata
typedef struct Nodo {
    int valore;             // Valore dell'intero
    struct Nodo* next;      // Puntatore al prossimo nodo
} Nodo;

// Alias per il tipo Nodo, per comodità
typedef Nodo* Lista;

int main() {
    
    return 0;
}